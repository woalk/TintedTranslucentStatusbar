<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.woalk.apps.xposed.ttsb"
    android:versionCode="9"
    android:versionName="Beta 2.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.woalk.apps.xposed.ttsb.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.woalk.apps.xposed.ttsb.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName="com.woalk.apps.xposed.ttsb.MainActivity"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name="com.woalk.apps.xposed.ttsb.ActivitiesActivity"
            android:label="@string/title_activity_activities"
            android:parentActivityName="com.woalk.apps.xposed.ttsb.MainActivity"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name="com.woalk.apps.xposed.ttsb.EasySettingsActivity"
            android:label="@string/title_activity_easy_settings"
            android:parentActivityName="com.woalk.apps.xposed.ttsb.ActivitiesActivity"
            android:launchMode="singleTask" >
        </activity>

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Custom system bar tinting using the KitKat gradient (&quot;translucent mode&quot;)." />
        <meta-data
            android:name="xposedminversion"
            android:value="30" />
        <meta-data
            android:name="tintengine_version"
            android:value="2.0" />
        <meta-data
            android:name="ui_version"
            android:value="2.0" />
    </application>

</manifest>
